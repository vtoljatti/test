const SET_ALL_ROLES = (state, roles) => {
    state.roles = roles
};

const SET_DATA_MODAL = (state, dataModal) => {
    state.dataModal = dataModal
};

export {
    SET_ALL_ROLES,
    SET_DATA_MODAL
}
